
#include <iostream>
#include <algorithm>
#include <cmath>        // abs() for float, and fabs()
#include <math.h>       // pow()
#include <random>
#include <climits>

#include <Eigen/Dense>

#define print(var) \
  std::cout<<#var" = "<<(var)<<std::endl;
#define printstr(str) \
  std::cout<<str<<std::endl;
#define printLine() \
  std::cout<<"============================"<<std::endl;
